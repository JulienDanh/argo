apiVersion: v1
kind: ConfigMap
metadata:
  name: example-config
  namespace: fastapi-app
  labels:
    app: fastapi-app
    managed-by: argocd

data:
  config.json: |
    {
      "message": "Hello from ArgoCD ConfigMap!",
      "enabled": true
    }
